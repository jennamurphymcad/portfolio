var sound, waveform, canvas;function preload(){  sound = loadSound('harp.wav');}function setup() {  canvas = createCanvas(600, 600);  fft = new p5.FFT();  canvas.mouseClicked(function() {    if (sound.isPlaying() ){      sound.stop();    } else {      sound.play();    }  });}function draw() {  var waveform = fft.waveform();  beginShape();  noFill();  strokeWeight(0.05);  for (var i = 0; i< waveform.length; i++){    var x = map(i, 0, waveform.length, 0, width);    var y = map(waveform[i]*random(1.75), -1, 1, 0, height);    vertex(x,y);  }  endShape();}